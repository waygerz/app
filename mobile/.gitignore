# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies

# android/ and ios/ are committed (app ID com.waygerz.app). Other platforms are
# unsupported — ignore them if `flutter create` ever generates them.
/linux/
/macos/
/windows/
/web/

# Firebase config generated by `flutterfire configure` (contains project keys)
lib/firebase_options.dart
android/app/google-services.json
ios/Runner/GoogleService-Info.plist

# IDE
.idea/
*.iml
.vscode/
