# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# Platform folders are generated by `flutter create .` (not checked in for the
# scaffold). Remove these lines once you commit real native config.
/android/
/ios/
/linux/
/macos/
/windows/
/web/

# Firebase config generated by `flutterfire configure` (contains project keys)
lib/firebase_options.dart
android/app/google-services.json
ios/Runner/GoogleService-Info.plist

# IDE
.idea/
*.iml
.vscode/
